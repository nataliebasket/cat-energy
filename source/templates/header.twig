<header class="header page__centerer">
  <div class="header__container">
    <div class="header__logo-container">
      <a class="header__logo" href="index.html" {% if permalink == "index.html" %}aria-current="page"{% endif %}>
        <picture>
          <source media="(min-width: 1280px)" srcset="img/mysprite.svg#logo-desktop">
          <source media="(min-width: 768px)" srcset="img/mysprite.svg#logo-tablet">
          <img src="img/mysprite.svg#logo-mobile" alt="Картинка магазина питания." width="33" height="38">
        </picture>
      </a>
      <a class="header__logo-none" href="index.html" {% if permalink == "index.html" %}aria-current="page"{% endif %}>
        <img src="img/mysprite.svg#logo-mobile-text" alt="Название магазина питания." width="101" height="18">
      </a>
    </div>

    <nav class="navigation navigation--closed navigation--nojs">
      <button class="navigation__button" type="button">
        <span class="page__hidden">Открыть меню.</span>
      </button>
      <div class="navigation__container">
        <ul class="navigation__list">
          {% for page in pages %}
            <li class="navigation__item">
              <a class="navigation__link {% if permalink == page.path %} navigation__link--active {% endif %} {% if permalink == "index.html" %} navigation__link--white {% endif %}" href="{{ page.path }}" {% if permalink == page.path %} aria-current="page" {% endif %}>{{ page.name }}</a>
            </li>
          {% else %}
            <li>Из навигации пропали все пункты 😕</li>
          {% endfor %}
        </ul>
      </div>
    </nav>
  </div>
</header>
